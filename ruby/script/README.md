# Scripts

Run the solution from the `ruby/` folder:

```bash
./demo.sh
```

Smoke-test inside the Dev Container image (requires Docker):

```bash
./script/test-devcontainer.sh
```

On **Windows PowerShell** (avoids Git Bash path conversion issues):

```powershell
./script/test-devcontainer.ps1
```
