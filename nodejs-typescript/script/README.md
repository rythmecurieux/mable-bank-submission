# Scripts

Run the solution from the `nodejs-typescript/` folder:

```bash
./demo.sh
```

Full CI-style check (format, lint, coverage, and demo): `npm run verify`.

Smoke-test inside the Dev Container image (requires Docker):

```bash
./script/test-devcontainer.sh
```
